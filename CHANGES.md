# Changes

## 0.1.0

- Forked from https://github.com/csteeg/kroki-preview-vscode and republished under the `agayev169` publisher.
- Added `kroki.serverUrl` setting so users can point the previewer at a self-hosted Kroki instance (e.g. `http://localhost:8000`) instead of the default `https://kroki.io`.

## 0.0.1 First test version

- Version alpha
